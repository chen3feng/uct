# Set line endings for batch files to CRLF (DOS format)
*.bat text eol=crlf

# Set line endings for shell scripts to LF (UNIX format)
*.sh text eol=lf
cltue text eol=lf

# Default behavior for all other files (unchanged)
* -text
